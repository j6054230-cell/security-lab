name: CI/CD Pipeline

permissions:
  contents: read

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    steps:
      - name: Checkout code
        uses: actions/checkout@692973e3d9371296bf4051010375179853907409
        with:
          fetch-depth: 1 # Tezkor checkout

      - name: Setup Node.js
        uses: actions/setup-node@1e60f6205b5464e83c7908b896f1345d3e09156a
        with:
          node-version: '20'

      # Caching orqali o'rnatish tezligini oshirish
      - name: Cache node modules
        uses: actions/cache@0c45773b623bea8c85750d4d29315d0506385d30
        with:
          path: ./frontend/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('./frontend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm install

      - name: Run Tests
        run: npm test

  security-scan:
    needs: build-and-test
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@692973e3d9371296bf4051010375179853907409
      
      - name: Initialize CodeQL
        uses: github/codeql-action/init@b6a47833a6932463e76a6e2e5050f55cf6a67f1b
        with:
          languages: javascript
          
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@b6a47833a6932463e76a6e2e5050f55cf6a67f1b
